---
phase: 05-foundation
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - client/src/api.js
  - client/src/main.js
autonomous: false
requirements: [API-01]

must_haves:
  truths:
    - "Client volume list loads correctly using /api/v1/ endpoints"
    - "Client label save/load works via /api/v1/ endpoints"
    - "Client segmentation save works via /api/v1/ endpoints"
  artifacts:
    - path: "client/src/api.js"
      provides: "API_BASE pointing to /api/v1"
      contains: "const API_BASE = '/api/v1'"
    - path: "client/src/main.js"
      provides: "All fetch calls using /api/v1/ prefix"
  key_links:
    - from: "client/src/api.js"
      to: "server/main.py"
      via: "HTTP fetch to /api/v1/volumes"
      pattern: "API_BASE.*api/v1"
    - from: "client/src/main.js"
      to: "server/main.py"
      via: "fetch calls to /api/v1/volumes/{id}/labels and /api/v1/volumes/{id}/segmentations"
      pattern: "/api/v1/volumes/"
---

<objective>
Update all client-side API URLs from /api/ to /api/v1/ prefix.

Purpose: Complete the API versioning migration (API-01) by updating the JavaScript client to use the new versioned endpoints. Without this, the client will 404 on all API calls.

Output: Client fully functional with versioned API endpoints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-foundation/05-RESEARCH.md
@.planning/phases/05-foundation/05-01-SUMMARY.md

<interfaces>
<!-- api.js current state -->
From client/src/api.js:
```javascript
const API_BASE = '/api';
export async function fetchVolumes() { ... }
export async function fetchVolumeMetadata(volumeId) { ... }
export async function fetchVolumeData(volumeId) { ... }
```

<!-- main.js hardcoded URLs (3 locations) -->
From client/src/main.js:
```javascript
// Line 87:  fetch(`/api/volumes/${volume.id}/labels`)
// Line 121: fetch(`/api/volumes/${volume.id}/labels`, { method: 'PUT', ... })
// Line 295: fetch('/api/volumes/' + metadata.id + '/segmentations?filename=...')
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all client API URLs to /api/v1/ prefix</name>
  <files>client/src/api.js, client/src/main.js</files>
  <read_first>
    - client/src/api.js (full file -- only 20 lines)
    - client/src/main.js (search for all `/api/` occurrences to find every URL)
  </read_first>
  <action>
1. In `client/src/api.js` line 1, change:
   FROM: `const API_BASE = '/api';`
   TO:   `const API_BASE = '/api/v1';`

2. In `client/src/main.js`, update ALL 3 hardcoded API URLs:
   - Line ~87: Change `/api/volumes/${volume.id}/labels` to `/api/v1/volumes/${volume.id}/labels`
   - Line ~121: Change `/api/volumes/${volume.id}/labels` to `/api/v1/volumes/${volume.id}/labels`
   - Line ~295: Change `'/api/volumes/' + metadata.id + '/segmentations` to `'/api/v1/volumes/' + metadata.id + '/segmentations`

3. After making changes, search the entire client/src/ directory for any remaining `/api/` references that do NOT include `/api/v1/` to ensure nothing was missed. The Vite proxy config (`vite.config.js`) should NOT be changed -- it proxies `/api` which still matches `/api/v1/`.

Note: Do NOT change vite.config.js. The proxy target `/api` matches `/api/v1/` paths since they start with `/api`.
  </action>
  <verify>
    <automated>cd /Users/bje/repos/NextEd && grep -rn "'/api/" client/src/ | grep -v "api/v1" | grep -v node_modules; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - client/src/api.js contains `const API_BASE = '/api/v1'`
    - client/src/api.js does NOT contain `const API_BASE = '/api'` (without v1)
    - client/src/main.js contains `/api/v1/volumes/` in all 3 fetch call locations
    - client/src/main.js does NOT contain `/api/volumes/` (without v1) in any fetch call
    - grep for `/api/` without `/api/v1/` in client/src/ returns zero results
    - vite.config.js is unchanged
  </acceptance_criteria>
  <done>All client API calls use /api/v1/ prefix. No unversioned /api/ URLs remain in client source.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full stack works with versioned API</name>
  <what-built>
    Complete API versioning migration across server and client:
    - All 8 server endpoints moved to /api/v1/ prefix
    - VolumeMetadata extended with study_instance_uid and series_instance_uid
    - DICOM discovery extracts StudyInstanceUID
    - Debug endpoint at /api/v1/debug/volumes/{id}/paths
    - Client API_BASE updated to /api/v1
    - All hardcoded URLs in main.js updated
  </what-built>
  <how-to-verify>
    1. Start the server: `cd server && uv run python main.py /path/to/your/data`
    2. Start the client: `cd client && npm run dev`
    3. Open http://localhost:5173 in browser
    4. Verify: Volume list loads (confirms /api/v1/volumes works)
    5. Click a volume to open it (confirms /api/v1/volumes/{id}/metadata and /data work)
    6. If you have DICOM volumes: check browser Network tab for /api/v1/volumes response -- should include study_instance_uid and series_instance_uid fields
    7. If segmentation editing is available: verify labels load and save (confirms /api/v1/volumes/{id}/labels works)
    8. Verify old URL returns 404: open http://localhost:8000/api/volumes in browser -- should show 404
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- `grep -rn "'/api/" client/src/ | grep -v "api/v1"` returns zero results
- `grep "API_BASE" client/src/api.js` shows `/api/v1`
- Manual test: volume list loads in browser
</verification>

<success_criteria>
- Client loads volume list, metadata, and data through versioned /api/v1/ endpoints
- No client-side 404 errors due to old API paths
- Labels load and save correctly
- Segmentation endpoints work
</success_criteria>

<output>
After completion, create `.planning/phases/05-foundation/05-02-SUMMARY.md`
</output>
