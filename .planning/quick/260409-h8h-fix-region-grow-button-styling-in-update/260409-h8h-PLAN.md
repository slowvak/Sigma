# Quick Task 260409-h8h: Fix Region Grow button styling in updateActiveTool

**Date:** 2026-04-09
**Status:** Ready

## Task

In `client/src/main.js`, the `updateActiveTool` function sets all inactive tool buttons to plain white (`#fff`). The Region Grow button uses the ✨ emoji which renders yellow on white — poor contrast. Fix: give the Region Grow button a green tint when inactive.

## Tasks

### Task 1: Update `updateActiveTool` in `client/src/main.js`

**File:** `client/src/main.js` (~line 539)

**Action:** In the `else` branch of `updateActiveTool`, check if `btn.getAttribute('data-tool') === 'region-grow'`. If so, apply green tint (`#e6f4ea` bg, `#2e7d32` color/border). Otherwise apply the existing plain white style.

**Done when:** The region-grow button shows green tint when inactive, and blue when active — no other buttons affected.
